---
import { Image } from "astro:assets";
import Right from "../../assets/icons/Right.svg"
import Button from "../ui/Button.astro";
---

<section class="max-w-7xl mx-auto px-5 mt-17 md:mt-36 space-y-6 md:space-y-11">

    <h1 class="text-neutral text-mobile-h3 md:text-desktop-h3 font-medium max-sm:leading-tight">Visa applications are confusing.</h1>
    
    <div class="flex max-md:flex-col justify-center lg:items-center gap-6 md:gap-11">
        <div class="flex-3/5 rounded-lg">
            <video
            autoplay
            muted
            loop
            playsinline
            preload="metadata"
            class="rounded-lg"
            >
                <source src="/visualproblem.mp4" type="video/mp4">
            </video>
        </div>

        <div class="flex-2/5 space-y-3 md:space-y-6">
            <h2 class="text-mobile-h4 md:text-desktop-h4 leading-9">Most rejections happen because:</h2>

            <ul class="my-4 text-mobile-h5 md:text-desktop-h5 font-light leading-tight space-y-2 md:space-y-4">
                <li class="flex items-center gap-2">
                    <Image
                        src={Right}
                        alt={"✔️"}
                        class="w-7 h-7"
                    />

                    The wrong visa category was selected
                </li>
                <li class="flex items-center gap-2">
                    <Image
                        src={Right}
                        alt={"✔️"}
                        class="w-7 h-7"
                    />

                    A form was filled incorrectly
                </li>
                <li class="flex items-center gap-2">
                    <Image
                        src={Right}
                        alt={"✔️"}
                        class="w-7 h-7"
                    />

                    A required document was misunderstood or missed
                </li>
                <li class="flex items-center gap-2">
                    <Image
                        src={Right}
                        alt={"✔️"}
                        class="w-7 h-7"
                    />

                    Instructions were unclear or contradictory
                </li>
            </ul>

            <Button
                href="#contact"
                label="Request Form Assistance"
                classes="inline-block mt-3 rounded-lg px-5 py-3 text-accent-green-800 border-2 border-accent-green-600 hover:bg-accent-green-500 hover:text-surface cursor-pointer text-mobile-h6 md:text-desktop-h6"
            />
        </div>

    </div>

</section>